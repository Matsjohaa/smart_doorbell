# Raspberry Pi hardware
gpiozero>=1.6.2
# picamera2 -- install via system package: sudo apt install python3-picamera2

# Web framework
flask>=2.0
flask-cors>=3.0

# Face recognition
face-recognition>=1.3.0
numpy>=1.21

# Push notifications (uses Expo Push Service — no extra packages needed)

# Image handling
Pillow>=9.0
